# VULNDB Nginx reverse proxy example
upstream vulndb_app {
    server 127.0.0.1:8000;
}

server {
    listen 80;
    server_name CHANGE_ME_HOSTNAME;

    client_max_body_size 32m;

    location /static/ {
        alias /opt/vulndb/staticfiles/;
    }

    location /media/ {
        alias /var/lib/vulndb/media/;
    }

    location / {
        proxy_pass http://vulndb_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# TLS example (after certbot / internal CA):
# server {
#     listen 443 ssl http2;
#     server_name CHANGE_ME_HOSTNAME;
#     ssl_certificate     /etc/ssl/vulndb/fullchain.pem;
#     ssl_certificate_key /etc/ssl/vulndb/privkey.pem;
#     ... same locations ...
# }
